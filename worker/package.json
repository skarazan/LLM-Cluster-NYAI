{
  "name": "llm-cluster-worker",
  "version": "1.0.0",
  "description": "Worker agent — self-registers with the LLM Cluster manager",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
